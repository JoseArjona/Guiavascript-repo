---
import Header from "../components/Header.astro";
import BaseHead from "../components/BaseHead.astro";
import "../styles/prism.css";
import Prismc from "../components/Prismc.astro";
import BaseNavDm from "../components/dom/BaseNavDm.astro";

export interface Props {
  title: string;
  description: string;
}

const { title } = Astro.props;
const { description } = Astro.props;
if (Astro.url.host === "localhost") return;
const URLBG = Astro.url.protocol + "//" + Astro.url.host + "/img/bgDom.svg";

const NamePagesDom = await Astro.glob("../pages/basic-doml/*.astro");
console.log(NamePagesDom);
console.log(Astro.url);
---

<!doctype html>
<html lang="es">
  <head>
    <BaseHead title={title} description={description} />
  </head>
  <body class="txt-white" style=`background-image: url(${URLBG});`>
    <Header NamePages={NamePagesDom} />
    <main class="container">
      <slot />
      <BaseNavDm NamePagesDom={NamePagesDom} />
    </main>
    <footer>
      <p>
        <a href="https://developer.mozilla.org/es/docs/Web/JavaScript"
          >Consulta más en MDN</a
        >
      </p>
      <Prismc codep="^_~" />
    </footer>
    <style>
      body {
        background-repeat: no-repeat !important;
        background-size: cover !important;
        background-position: center !important;
      }
      footer {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #000;
      }
      footer p a {
        color: #ffffff;
        text-decoration: none;
        font-size: 1.2rem;
        margin: 0;
        margin-right: 10px;
      }
    </style>
  </body>
</html>
